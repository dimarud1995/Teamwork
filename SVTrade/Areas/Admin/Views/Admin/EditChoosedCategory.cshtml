@model SVTrade.Models.ChoosedCategory

@{
    ViewBag.Title = "Адмін: змінити категорію";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<h2>Змінити категорію</h2>


@using (Html.BeginForm("EditChoosedCategory", "Admin"))
{
    <div class="form-group">
        <div class="col-md-10">

           
            <div class="editor-field">
                @Html.EditorFor(x => x.chosenCategoryID, new { htmlAttributes = new { @class = "form-control" } })
            </div>

            <div class="editor-label">
                @Html.LabelFor(x => x.userID)
            </div>

            <div class="dropdown">
                @Html.DropDownListFor(m => m.userID,
                    (IEnumerable<SelectListItem>)ViewData["AllUsers"], "choose", new { @class = "form-control" })
            </div>

            <div class="editor-label">
                @Html.LabelFor(x => x.productCategoryID)
            </div>

            <div class="dropdown">
                @Html.DropDownListFor(m => m.productCategoryID,
                    (IEnumerable<SelectListItem>)ViewData["AllCategories"], "choose", new { @class = "form-control" })
            </div>

            <div class="editor-label">
                <input type="submit" value="Зберегти" class="btn btn-default" />
            </div>
            <div class="editor-field">
                @Html.ActionLink("Відмінити і повернутись до списку", "ChoosedCategoriesList")
            </div>
        </div>
    </div>

}


